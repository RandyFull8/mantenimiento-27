// import { Pipe, PipeTransform } from '@angular/core';
// import { ServiceModel } from '../models/serviceModel';

// @Pipe({
//   name: 'filterPagination'
// })
// export class FilterPaginationPipe implements PipeTransform {

//   transform(solicitud: ServiceModel[],search:string='', page:number=0): ServiceModel[] {
//     const filteredSolicitudes = solicitud.filter(soli=>soli.area.includes(search))
//     if(search.length===0){}
    
     
//       else
//         return solicitud.splice(page,page+5)
      
//          return filteredSolicitudes.slice(page,page+5)
      
      
   
   
//   }

// }
